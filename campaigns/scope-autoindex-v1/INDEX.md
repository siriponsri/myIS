# SCOPE AutoIndex Campaign

Canonical campaign configuration is `../../control/campaigns/scope-autoindex-v1.yaml`.
Run facts are hash-bound manifests and aggregate bundles; projections are
generated and never edited by hand.

Repository-safe P2 Owner-review drafts live under `proposals/`; validated
aggregate-only Owner-local preflight receipts live under `preflight/`. Neither
surface registers a measured candidate, opens selection, or changes real P2
counters by itself.
