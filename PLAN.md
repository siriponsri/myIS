# Active Plan Pointer

The active plan is defined by `control/layout.v2.yaml` and
`control/campaigns/scope-autoindex-v1.yaml` on the `main` branch.

Legacy C/S plans are preserved under `archive/legacy-cs/` and are not active.

Execution order: `V0 foundation -> V1 reversible fixture slice -> V2 SCOPE development -> V3 D2 final -> V4 publication -> D3 release`. D1 already authorizes V0-V2 preparation and development within the protected boundary and budget. No intermediate Owner gate exists.
