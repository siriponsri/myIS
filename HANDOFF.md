# myIS Research Migration Handoff

## Scope

Migration base: `4dd0f4b5698174a128d3d1c4d4efcdee6dd04f4c`.
This change migrates documentation, configuration, schemas, tests, dashboard,
MLflow, Brain/Linear projections, and IEEE manuscript skeletons only. It does not
run retrieval, optimization, qrels evaluation, GPU work, paid APIs, transfer,
confirmation, or publication.

## Active program

- Identity: `myIS Research` / `myis-research` / protocol `1.0`.
- Track versions: C `0.1`, S `0.1`; package `0.1.0`.
- Active flow: Track C -> frozen C1 harness -> Track S -> joint test -> PC/PS.
- Shared commitment: seed 42, counts 250/125/872; independent track firewalls.
- Independent ranking/evidence work is historical only; ranking is a C diagnostic.

## Blocking Owner decisions

- `C_MARGIN_VALUES_TBD_BLOCKING`
- `C_SOEI_VALUE_TBD_BLOCKING`
- `S_MARGIN_VALUES_TBD_BLOCKING`
- `COREWEAVE_FINAL_FREEZE_TBD_BLOCKING`
- `CT_BUDGET_LICENSE_TBD_BLOCKING`

Exact OUT-positive counts and split hashes also require the protected Owner-run
process. None of these values may be inferred from silence.

## Protected surfaces

The migration must leave `01_evidence/**`, PDFs/digests/provenance, the Brain
literature collection and Paper D boundary, `03_experiments/V01_brain_drive_agent_demo/**`,
`04_outputs/**`, immutable approvals/history, measured manifests/results,
`uv.lock`, `.python-version`, package version, App data/models/prompts/pools, and
unrelated `.codex/config.toml` untouched.

## Verified status and next authorized work

- `F0 = closed`; all three F0 tasks have validated task evidence.
- `G0 = approved` by the immutable scoped Owner record.
- `F1 = waiting_gate` and `G1 = pending`.
- Project readiness is `F1/G1 preparation only`.

The next authorized work is non-executable F1/G1 preparation: draft contracts,
schema validation, fail-closed adapter scaffolding, documentation, and local
projection updates. `F1.1` cannot run until the Owner records G1 approval for
the corpus, queries, qrels, family mapping, evaluator, field protocol,
published targets, compute budget, exact split membership/hash, exact
OUT-positive counts, and reproduction scope. Exact membership/counts must come
from the protected Owner process; estimates are not accepted. This system
supports retrieval decisions and is not legal advice.

## MLflow runtime boundary

The approved MLflow runtime store is outside Git and remains a non-authoritative
mirror. F0 bootstrap is limited to connectivity and records no scientific run,
dataset access, artifacts, or scientific metrics. Do not replay or mirror
governance documents during F1/G1 preparation without a separate Owner approval.

## Projection receipts

- Brain local-only commit: `8706ac0` on Brain `main`; serial-writer lease was
  acquired, validated, committed, and released. QMD indexed/searches the new C-S
  notes; 10 vector embeddings remained pending after the bounded 120-second CPU
  refresh timed out. Literature validation still passes for all 153 notes.
- Linear project: `myIS Research 1.0`, external ID
  `d866c23b-5b9f-422b-93cb-b0b6f6400fd4`; milestones F0 through PS and issues
  SIR-5 through SIR-26 mirror all 22 PLAN tasks. SIR-1 through SIR-4 were preserved.
- Canonical PLAN SHA-256 bound into both projections:
  `27361e079c9fe1c5f2c42ced99af4bc1367de8070a6393d34c0791f576c06e38`.
