# Track C - Candidate Exposure

Status: planning; not authorized to execute.

Required output before implementation: three ranked candidate paths expressed
as a KM graph, each with evidence, dataset requirements, exposure mechanism,
budget, latency, risks, falsification test and downstream dependency on Track R.
The Owner selects one path. Metrics and gates are defined in
`docs/governance/PUBLICATION_ROADMAP.md`.

