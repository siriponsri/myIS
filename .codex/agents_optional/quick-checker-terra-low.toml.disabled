# OPTIONAL ONLY — intentionally disabled by default.
name = "quick_checker"
description = "Bounded mechanical summarizer/checker. Prefer deterministic scripts first."
model = "gpt-5.6-terra"
model_reasoning_effort = "low"
sandbox_mode = "read-only"

developer_instructions = """
Use only for a short, bounded mechanical task that does not require scientific
judgment or visual interpretation. Never use for claim validation, reviewer decisions,
experiment design, or final submission readiness.
"""
