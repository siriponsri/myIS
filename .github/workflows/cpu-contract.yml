name: myIS CPU contract

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTHONUNBUFFERED: "1"
      MYIS_NETWORK_DISABLED: "1"
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v6
        with:
          version: "0.8.17"
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Install locked dependencies
        run: uv sync --locked --all-extras
      - name: Run synthetic and deterministic tests
        run: uv run --no-sync pytest -q
      - name: Validate active layout and projections
        run: |
          uv run --no-sync python scripts/validate_layout_v2.py
          uv run --no-sync myis-report check --read-model-only --repository-root .
      - name: Validate P2 readiness contract without execution
        run: |
          uv run --no-sync pytest -q tests/test_p2_budget_contracts.py tests/test_p2_freeze_barrier.py tests/test_p2_cli.py tests/test_p2_dashboard_projection.py tests/test_p2_mlflow_projection.py tests/test_provider_switching_static.py
          uv run --no-sync python -c "from pathlib import Path; from myis_research.p2.contracts import load_profile; from myis_research.p2.measured_contracts import load_profile_uri; p=load_profile(Path('.')); assert p.payload['limits']['max_candidates_total']==32; assert p.payload['runtime']['max_wall_clock_seconds']==259200; assert p.payload['runtime']['per_candidate_timeout_seconds']==10800; v2,_=load_profile_uri(Path('.'), 'control/budgets/p2-r1-primary-v2.yaml'); assert v2['limits']['max_candidates_total']==32; assert v2['runtime']['max_wall_clock_seconds']==432000; assert v2['runtime']['measurement_budget_seconds']==345600; assert v2['runtime']['overhead_reserve_seconds']==86400; assert v2['runtime']['per_candidate_timeout_seconds']==10800"
          uv run --no-sync python scripts/build_p2_measured_controls.py --repository-root . --check
      - name: Keep P2 selection and protected stores closed
        run: uv run --no-sync python .github/scripts/check_policy.py --repository-root . p2-closure
      - name: Reject archive runtime coupling
        run: uv run --no-sync python .github/scripts/check_policy.py --repository-root . archive-runtime
      - name: Check diff whitespace
        run: git diff --check
