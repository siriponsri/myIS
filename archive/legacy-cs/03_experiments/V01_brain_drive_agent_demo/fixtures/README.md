# Offline fixtures

The demo creates deterministic PDF and history fixtures at runtime and uses a
stable URL identifier for the web source. No source content is copied into
Brain or MLflow artifacts.
