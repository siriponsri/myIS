# Claude Instructions for myIS Research

Read `AGENTS.md`, `PLAN.md`, `FULL_RESEARCH_TRACK_PLAN.md`, and the applicable
Owner Gate before acting. Use active identity `myIS Research`, machine ID
`myis-research`, protocol `1.0`, with Track C/S version `0.1`.

The active path is `Track C -> frozen C1 harness -> Track S`. The historical
ranking/evidence lane is legacy; ranking is a Track C diagnostic and evidence
transfer is deferred.
Paper D and historical identity strings remain frozen where they are evidence.

Do not expose confirmation membership, qrels, payloads, or per-query outcomes.
Do not run experiments, paid APIs, GPU work, or publication actions without the
named Owner Gate. Brain, Linear, dashboard, and MLflow are projections, not paper
truth. No silent model/provider fallback is permitted.
