# Historical Track R - Ranking and Evidence

This directory preserves the former independent Track R description. It is
historical material only and is not an active execution path for `myIS Research`
protocol `1.0`.

The active program has two tracks: Track C and Track S. Pool ordering, a frozen
reranker, oracle/reachable nDCG, promotion/demotion, and failure attribution are
now the `C_DIAGNOSTIC` within Track C after the C0/C1 candidate pool is frozen.
They cannot create an independent gate or claim. Claim/passage evidence work is
deferred to a separately gated qualitative appendix or transfer lane.

No historical content in this directory authorizes a run, changes protected
data, or supersedes the active plans. This system is decision support, not legal
advice.

## Preserved Historical Description

The text below is retained verbatim as historical context. It does not describe
the current protocol.

# Track R - Ranking and Evidence

Track R receives the exact CF candidate-pool SHA-256 and may not expand, remove,
or substitute candidate families.

Primary claim: a selected reranker increases family-level `OUT nDCG@100` over
the preregistered no-rerank baseline on the identical frozen pool. Gate R is
independent of Gate C. Selection requires a strictly greater primary score and
rejects ties.

R0 measures oracle/reachable headroom. R1 develops practical, passage-aware, and
claim-limitation ranking with controls. R2 produces publication-level verbatim
evidence and an error taxonomy without making novelty/FTO legal conclusions.
PageIndex is optional only as a BM25/dense-routed within-document evidence pilot.

See `PLAN.md` Phases R0-R2 and `FULL_RESEARCH_TRACK_PLAN.md`.
