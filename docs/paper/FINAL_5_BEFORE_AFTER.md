# Final Five-Point Before/After

Scope: only the five closing points requested for the final layout pass. No
numbers, citations, labels, figures, protected sentences, or paragraph
boundaries were changed.

1. V-A frozen-system specification and render repair

   Before: `The preregistered rule selects the research system; the research
   system and static comparator are fixed at that close, with complete
   specifications in Table~\\ref{tab:systems}.`

   After: `The preregistered rule selects the research system; the research
   system and static comparator are fixed when it closes, with complete
   specifications in Table~\\ref{tab:systems}.`

   The `tab:systems` table was already present in the source and remains the
   sole location for the complete frozen specifications.

2. Restored sentence

   Before/after: `But precision is not leverage.` (already present; no source
   delta in this pass.)

3. Restored conclusion opening

   Before/after: `Three results, one lesson:` (already present; no source delta
   in this pass.)

4. Restored phrases

   Before/after: `is allowed to confirm` and `tells the same story from another
   angle` (already present; no source delta in this pass.)

5. Section III retrieval-depth clause

   Before/after: `unchanged except retrieval depth, extended to 200 for the
   diagnostic pool` (already present; no source delta in this pass.)
