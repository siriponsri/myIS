# Final Five-Point Before/After

Scope: only the five requested restorations and their layout repair. No
scientific number, citation, label, figure, protected sentence, or paragraph
boundary changed.

1. V-A frozen-system specification and render repair

   Before: `The preregistered rule selects \\selectedSpec; the comparator
   \\comparatorSpec is fixed at the same moment.`

   After: the complete `\\selectedSpec` and `\\comparatorSpec` values appear in
   Table~`\\ref{tab:systems}`, and the paragraph reads: `The preregistered rule
   selects the research system; the research system and static comparator are
   fixed when it closes, with complete specifications in
   Table~\\ref{tab:systems}.`

   Verification note: the table-based repair and revised sentence were already
   present at the start of this closeout; the rebuilt PDF confirms that the two
   reported rendering defects are absent.

2. Restored sentence

   Before: `Precision in reporting does not change the ordering.`

   After: `But precision is not leverage.`

   Verification note: the restored form was already present at the start of
   this closeout, so this round introduced no source delta for item 2.

3. Restored conclusion opening

   Before: `The unit that deserves a name in cross-domain patent retrieval is
   the retriever--representation configuration, not the retriever alone.`

   After: `Three results, one lesson: the unit that deserves a name in
   cross-domain patent retrieval is the retriever--representation
   configuration, not the retriever alone.`

   Verification note: the restored opening was already present at the start of
   this closeout, so this round introduced no source delta for item 3.

4. Restored phrases

   Before: `A composition control gives the same result.`

   After: `A composition control tells the same story from another angle.`

   Before: `We keep the levels apart: the transfer matrix shapes our
   interpretation; only the held-out comparison may provide confirmation.`

   After: `We keep the two levels apart on purpose: the transfer matrix shapes
   how we read the result, but only the held-out comparison is allowed to
   confirm.`

   Verification note: both restored phrases were already present at the start
   of this closeout, so this round introduced no source delta for item 4.

5. Section III retrieval-depth clause

   Before: `After confirmation, the winner alone is applied unchanged to all
   1,247 queries at depth 200, and every later analysis reuses that one Top-200
   pool with no further retrieval or model selection.`

   After: `After confirmation, the winner alone is applied to all 1,247 queries
   unchanged except retrieval depth, extended to 200 for the diagnostic pool;
   every later analysis reuses that one Top-200 pool with no further retrieval
   or model selection.`

   Verification note: the requested clause was already present at the start of
   this closeout, so this round introduced no source delta for item 5.

Layout-only closeout: the final bibliography page uses balanced IEEE columns.
The unresolved anonymous-link marker and its unused display macro were removed
so the final source and rendered text contain no unresolved placeholder marker.
