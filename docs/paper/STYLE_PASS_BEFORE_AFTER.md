# Style Pass Before/After

Scope: sentence-level word choice, word order, and removal of unnecessary wording. Paragraph boundaries, claims, numbers, citations, labels, figures, and protected sentences were preserved.

1. Before: `A composition control tells the same story from another angle.`
   After: `A composition control gives the same result.`

2. Before: `At the coarse, field level, then, representation is a passenger, not the driver---and no single transfer cell carries confirmatory weight, so we do not read one.`
   After: `At the coarse, field level, representation is a passenger, not the driver; no single transfer cell carries confirmatory weight.`

3. Before: `That settles the development question and sets up a sharper one.`
   After: `That settles development and sets up a sharper question.`

4. Before: `The configuration we would pick under this protocol looks strong on development data.`
   After: `The configuration looks strong on development data.`

5. Before: `Four frozen profiles enter a single Selection-125 evaluation, and all produce complete rankings;`
   After: `Four frozen profiles enter a single Selection-125 evaluation with complete rankings;`

6. Before: `The preregistered rule picks the research configuration (\\selectedSpec), and the comparator is \\comparatorSpec, fixed at the same moment.`
   After: `The preregistered rule picks the research configuration (\\selectedSpec); the comparator \\comparatorSpec is fixed at the same moment.`

7. Before: `Once Selection-125 closes, both complete systems---model, representation, prompt, program, runtime---are locked before anyone touches Final-872.`
   After: `After Selection-125, both complete systems---model, representation, prompt, program, runtime---are locked before Final-872.`

8. Before: `Both systems run all 872 queries without failure, and we verify deterministic replay.`
   After: `Both systems run all 872 queries without failure; deterministic replay is verified.`

9. Before: `Development and selection were closed before any of this was observed, which is what lets the comparison carry weight.`
   After: `Development and selection were closed before these outcomes were observed, allowing the comparison to carry weight.`

10. Before: `It is the paper's strongest confirmatory evidence, and it is deliberately system-level:`
    After: `It is the paper's strongest confirmatory evidence and is deliberately system-level:`

11. Before: `the design fixes representation together with every other component, so the win belongs to the complete configuration, not to representation on its own.`
    After: `the design fixes representation with every other component, so the win belongs to the complete configuration, not representation alone.`

12. Before: `It also, on the strict cross-domain slice, still misses most of what it should find---and a confirmed system is the right place to ask why.`
    After: `On the strict cross-domain slice, it still misses most of what it should find---a confirmed system is the right place to ask why.`

13. Before: `This strict cross-domain population differs from Final-872, so we report it as a separate characterization rather than a continuation of the confirmation.`
    After: `This strict cross-domain population differs from Final-872, so we report it separately from the confirmation.`

14. Before: `how much of the remaining error could a perfect reordering of the existing candidates recover?`
    After: `how much remaining error could a perfect reordering of existing candidates recover?`

15. Before: `Three results, one lesson: the unit that deserves a name in cross-domain patent retrieval is the retriever--representation configuration, not the retriever alone.`
    After: `The unit that deserves a name in cross-domain patent retrieval is the retriever--representation configuration, not the retriever alone.`

16. Before: `and those choices deserve the precision we already give a checkpoint name or a retrieval depth.`
    After: `these choices deserve the precision of a checkpoint name or retrieval depth.`

17. Before: `The protected comparison moves the burden of proof from development behavior to a complete system, and the system holds up on held-out queries with development already closed.`
    After: `The protected comparison moves the burden of proof from development behavior to a complete system, which holds up on held-out queries with development already closed.`

18. Before: `We keep the two levels apart on purpose:`
    After: `We keep the two levels apart:`

19. Before: `The confirmed system, finally, points to where the next gain lives---and it is not the ranker.`
    After: `The confirmed system points to where the next gain lives---and it is not the ranker.`

20. Before: `A few boundaries.`
    After: *(deleted; the paragraph now opens with its substantive boundary statement.)*

21. Before: `A retrieval score reflects two things at once---the model that scores the evidence and the construction that decides what evidence exists---and once the first-stage configuration is fixed, a third constraint appears: which relevant families ever enter the pool.`
    After: `A retrieval score reflects both the model that scores evidence and the construction that decides what evidence exists. Once the first-stage configuration is fixed, a further constraint appears: which relevant families enter the pool.`

22. Before: `Together they set both the right reporting unit and the honest limit of anything a downstream ranker can claim.`
    After: `Together they set the reporting unit and the honest limit of downstream ranking.`

23. Before: `ARM-03 PatEmbed-large (datalyes/patembed-large, revision 2d5c0f92a3e5dc3d5415c08e612c57543c0e03ad); a2-arm-03-matched-b2-orthogonal title+abstract+claims construction with 384-token passages, 64-token overlap, NFKC whitespace, mean non-padding pooling, L2 normalization, and max-p family aggregation; query prefix ``encode query for different document retrieval:''; no fusion, top-100.`
    After: `ARM-03: datalyes/patembed-large (revision 2d5c0f92a3e5dc3d5415c08e612c57543c0e03ad); a2-arm-03-matched-b2-orthogonal, title+abstract+claims, 384-token passages with 64-token overlap, NFKC whitespace, mean non-padding pooling, L2 normalization, max-p family aggregation; query prefix ``encode query for different document retrieval:''; no fusion, top-100.`

24. Before: `Four frozen profiles enter a single Selection-125 evaluation with complete rankings; cross-domain selection statistics use the 90 queries with positive cross-domain relations. The preregistered rule picks the research configuration (\\selectedSpec); the comparator \\comparatorSpec is fixed at the same moment.`
    After: `Four frozen profiles enter one Selection-125 evaluation with complete rankings; cross-domain selection statistics use 90 queries with positive cross-domain relations. The preregistered rule selects \\selectedSpec; the comparator \\comparatorSpec is fixed at the same moment.`

25. Before: `The protected comparison moves the burden of proof from development behavior to a complete system, which holds up on held-out queries with development already closed. We keep the two levels apart: the transfer matrix shapes how we read the result, but only the held-out comparison is allowed to confirm.`
    After: `The protected comparison moves the burden of proof from development behavior to a complete system that holds up on held-out queries with development closed. We keep the levels apart: the transfer matrix shapes our interpretation; only the held-out comparison may provide confirmation.`
